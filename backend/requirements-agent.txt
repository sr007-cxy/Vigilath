# GEO 优化 Agent 独立 service 的依赖(方案 B,独立 venv,与主后端隔离)。
# 安装:python -m venv agent-venv && agent-venv/bin/pip install -r requirements-agent.txt
# 本地已验证此版本组合能 import 整条 geo 服务/模型层 + 构造 Pydantic AI agent。
#
# 关键:fastapi>=0.115 才兼容 pydantic>=2.11;pydantic-ai 需 pydantic>=2.11。
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.11
pydantic-settings>=2.6
pydantic-ai-slim[openai]>=1.0,<2

# geo 服务/模型层(被 agent 工具复用)所需,与主后端版本对齐:
sqlalchemy==2.0.23
psycopg[binary]>=3.1          # geo.database 用 postgresql+psycopg://(import 时即建 engine)
requests==2.31.0
beautifulsoup4==4.12.2
python-jose==3.3.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.6
python-dotenv==1.0.0
httpx>=0.25.0
redis>=5.0
aiohttp==3.9.1
email-validator
