version: '3.8'

services:
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    depends_on:
      - backend
    environment:
      - VITE_API_URL=http://backend:8070/api

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    ports:
      - "8070:8070"
    environment:
      - PYTHONUNBUFFERED=1
