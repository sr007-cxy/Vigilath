// TODO(P2-followup): render a shield icon + green/red state + one-line verdict
// from HTTPS checks. Returning null here makes CategoryVisual fall back to the
// plain row list, which is acceptable for the stub phase.
export function HttpsCard() {
  return null;
}
